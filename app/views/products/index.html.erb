<h1>Cart</h1>

<ul>
  <% cart.each do |product| %>
    <li><%= product %></li>
<% end %>
</ul>

<%= form_tag do %>
  <%= text_field_tag :product, nil, placeholder: "Add Product" %>
  <%= submit_tag "add to cart" %>
<% end %> 